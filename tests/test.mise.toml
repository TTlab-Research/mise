[tools]
python = "3.12"
node = "22"
rust = "stable"

[env]
NODE_ENV = "development"
VIRTUAL_ENV = "{{ cwd }}/.venv"

[tasks.dev]
description = "Run dev server"
run = "npm run dev"
depends = ["install"]

[tasks.test]
run = "pytest"

[hooks.enter]
run = "uv sync --quiet"
